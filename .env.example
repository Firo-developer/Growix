# Server-only key for the OpenRouter assistant route. Never expose it with a
# NEXT_PUBLIC_ prefix or place it in client-side code.
OPENROUTER_API_KEY="sk-or-v1-your-key"

# Used for OpenRouter app attribution. Set this to the deployed app URL.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
